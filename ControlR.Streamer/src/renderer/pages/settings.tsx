import { Alert } from "react-bootstrap";

export default function Settings() {
    return (
        <Alert variant="info">
            There are no settings yet.  Check back later!
        </Alert>
    )
}