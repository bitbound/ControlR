# ControlR.Libraries.Api.Contracts

This library contains the data structures and interfaces used for communication between the ControlR API clients and server.

It's generally meant to be consumed as a transitive dependency of [ContorlR.ApiClient](https://www.nuget.org/packages/ControlR.ApiClient).