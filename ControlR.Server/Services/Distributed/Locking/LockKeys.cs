namespace ControlR.Server.Services.Distributed.Locking;

public static class LockKeys
{
    public const string AgentCount = nameof(AgentCount);
    public const string ViewerCount = nameof(ViewerCount);
}
