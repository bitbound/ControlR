@attribute [Route(ClientRoutes.Permissions)]
@attribute [Authorize(Roles = RoleNames.TenantAdministrator)]

<PageTitle>Permissions</PageTitle>

<PrimaryTitle>
  Permissions
</PrimaryTitle>

<MudTabs PanelClass="pa-4">
  <MudTabPanel Text="Tags" Icon="@(Icons.Material.Filled.Label)" IconColor="Color.Secondary">
    <TagsTabContent />
  </MudTabPanel>
  <MudTabPanel Text="Users" Icon="@(Icons.Material.Filled.AccountCircle)" IconColor="Color.Secondary">
    <UsersTabContent />
  </MudTabPanel>
  <MudTabPanel Text="Devices" Icon="@(Icons.Material.Filled.Devices)" IconColor="Color.Secondary">
    <DevicesTabContent />
  </MudTabPanel>
</MudTabs>
